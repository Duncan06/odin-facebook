<%= form_with model: @post do |f| %>
  <h4>Create a post</h4>
  <p>
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.label :image %>
    <%= f.file_field :image %>
  </p>
  <p>
    <%= f.submit "Post" %>
  </p>
<% end %>
